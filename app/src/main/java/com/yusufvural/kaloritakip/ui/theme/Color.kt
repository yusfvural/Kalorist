package com.yusufvural.kaloritakip.ui.theme

import androidx.compose.ui.graphics.Color

// Profile Redesign Colors
// Profile Redesign Colors
val PrimaryRed = Color(0xFFF20D0D) // Reverted to Energy Red
// val BrandCream = Color(0xFFF4EBD0) // Removed
val PrimaryContainer = Color(0xFFFFF0F0) // Very light red for backgrounds

val BackgroundLight = Color(0xFFF9FAFB) // Neutral 50
val BackgroundDark = Color(0xFF111827) // Gray 900
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1F2937) // Gray 800
val TextSecondaryLight = Color(0xFF6B7280) // Gray 500
val TextSecondaryDark = Color(0xFF9CA3AF) // Gray 400
val OutlineLight = Color(0xFFE5E7EB) // Gray 200
val OutlineDark = Color(0xFF374151) // Gray 700

// Legacy / App-wide fallback
val EnerjikKirmizi = PrimaryRed // Alias for compatibility
val BackgroundWhite = SurfaceLight
val SurfaceGrey = BackgroundLight
val TextDark = Color(0xFF1A1A1A)
val LightGrey = Color(0xFFE0E0E0)
val TextPrimary = Color(0xFF2D3436)
val TextSecondary = Color(0xFF636E72)
val GlassWhite = Color.White.copy(alpha = 0.7f)
val GlassBorder = Color.White.copy(alpha = 0.5f)
val GradientStart = Color(0xFFFFF0F5)
val GradientEnd = Color(0xFFFFFFFF)
val SoftRed = Color(0xFF8C242C) // Lighter Burgundy
val SoftPink = Color(0xFFB84B55)
val PremiumCardGradientStart = Color(0xFFFFFFFF)
val PremiumCardGradientEnd = Color(0xFFFAFAFA) // Very subtle off-white
val PremiumShadow = Color(0x14000000) // 8% Black transparency for soft shadow
val PrimaryGradientStart = Color(0xFFE31E24)
val PrimaryGradientEnd = Color(0xFFFF5252)

val SuccessGreen = Color(0xFF00C49F)
val NutrientFat = Color(0xFFFFBB28)
val NutrientProtein = Color(0xFFE31E24)
val NutrientCarb = Color(0xFF00C49F)